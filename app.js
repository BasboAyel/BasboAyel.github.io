const soundToggle=document.querySelector('#soundToggle');let audioContext;soundToggle?.addEventListener('click',()=>{const on=soundToggle.getAttribute('aria-pressed')==='true';soundToggle.setAttribute('aria-pressed',String(!on));soundToggle.textContent=`sound: ${on?'off':'on'}`;if(!on){audioContext||=new AudioContext();const o=audioContext.createOscillator(),g=audioContext.createGain();o.frequency.value=440;g.gain.setValueAtTime(.025,audioContext.currentTime);g.gain.exponentialRampToValueAtTime(.001,audioContext.currentTime+.25);o.connect(g).connect(audioContext.destination);o.start();o.stop(audioContext.currentTime+.25)}});
const canvas=document.querySelector('#universe');if(canvas&&!matchMedia('(prefers-reduced-motion: reduce)').matches){const ctx=canvas.getContext('2d');let dots=[],mouse={x:-500,y:-500},warp=1;const setup=()=>{canvas.width=innerWidth*devicePixelRatio;canvas.height=(innerHeight-123)*devicePixelRatio;canvas.style.height=`${innerHeight-123}px`;ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);dots=Array.from({length:Math.min(90,innerWidth/13)},()=>({x:Math.random()*innerWidth,y:Math.random()*(innerHeight-123),vx:(Math.random()-.5)*.35,vy:(Math.random()-.5)*.35,r:Math.random()*2+1}))};const draw=()=>{ctx.clearRect(0,0,innerWidth,innerHeight);dots.forEach((d,i)=>{const dx=d.x-mouse.x,dy=d.y-mouse.y,dist=Math.hypot(dx,dy);if(dist<150){d.vx+=dx/dist*.008;d.vy+=dy/dist*.008}d.x+=d.vx*warp;d.y+=d.vy*warp;d.vx*=.992;d.vy*=.992;if(d.x<0||d.x>innerWidth)d.vx*=-1;if(d.y<0||d.y>innerHeight)d.vy*=-1;ctx.beginPath();ctx.arc(d.x,d.y,d.r,0,7);ctx.fillStyle=i%3?'rgba(65,42,95,.48)':'rgba(255,109,133,.7)';ctx.fill();dots.slice(i+1).forEach(o=>{if(Math.hypot(d.x-o.x,d.y-o.y)<105){ctx.beginPath();ctx.moveTo(d.x,d.y);ctx.lineTo(o.x,o.y);ctx.strokeStyle='rgba(65,42,95,.12)';ctx.stroke()}})});requestAnimationFrame(draw)};addEventListener('resize',setup);addEventListener('pointermove',e=>mouse={x:e.clientX,y:e.clientY-78});document.querySelector('#warpButton')?.addEventListener('click',()=>{warp=9;setTimeout(()=>warp=1,700)});setup();draw()}
document.querySelectorAll('.filter-button').forEach(button=>button.addEventListener('click',()=>{const tag=button.dataset.filter;document.querySelectorAll('.filter-button').forEach(b=>b.setAttribute('aria-pressed',b===button));document.querySelectorAll('.project-card').forEach(card=>card.classList.toggle('hide',tag!=='all'&&!card.dataset.tags.includes(tag)))}));const dialog=document.querySelector('#bookDialog');document.querySelectorAll('.book').forEach(book=>book.addEventListener('click',()=>{document.querySelector('#bookTitle').textContent=book.dataset.title;document.querySelector('#bookText').textContent=book.dataset.text;dialog?.showModal()}));document.querySelector('#closeDialog')?.addEventListener('click',()=>dialog.close());
